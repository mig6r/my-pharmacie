{% extends "base.html.twig" %}

{% block title %}
    {{ medicament.name }}
{% endblock %}

{%  block body %}
    <div class="container mt-4">
        <div class="row">
            <div class="col-md-6">

            </div>
            <div class="col-md-6">
                <h1>{{ medicament.name }}</h1>
                <div class="text-primary">{{ medicament.description }}</div>
            </div>
        </div>
    </div>
{% endblock %}